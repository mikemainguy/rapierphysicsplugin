{
  "name": "rapierphysicsplugin",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w packages/client -w packages/server -w packages/demo",
    "test": "vitest run",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist packages/*/*.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^3.0.0"
  }
}
