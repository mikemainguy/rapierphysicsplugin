{
  "name": "rapierphysicsplugin",
  "version": "1.0.13",
  "packageManager": "yarn@4.13.0",
  "license": "MIT",
  "description": "shared code for rapier client and server",
  "keywords": [
    "babylonjs",
    "physics",
    "server",
    "rapier",
    "multiuser"
  ],
  "bugs": {
    "url": "https://github.com/mikemainguy/rapierphysicsplugin/issues"
  },
  "author": {
    "name": "Michael Mainguy",
    "email": "mike.mainguy@gmail.com",
    "url": "https://www.npmjs.com/~mmainguy"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -At run build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist packages/*/*.tsbuildinfo",
    "version:sync": "node -e \"const v=require('./package.json').version;const fs=require('fs');const pkgs=fs.readdirSync('packages').filter(d=>fs.statSync('packages/'+d).isDirectory());pkgs.forEach(p=>{const f='packages/'+p+'/package.json';const j=JSON.parse(fs.readFileSync(f));j.version=v;for(const dep of ['dependencies','devDependencies','peerDependencies']){if(j[dep]){for(const k of Object.keys(j[dep])){if(k.startsWith('@rapierphysicsplugin/')){j[dep][k]=v}}}}fs.writeFileSync(f,JSON.stringify(j,null,2)+'\\n')})\"",
    "publish:all": "yarn version:sync && yarn build && yarn workspaces foreach -A --no-private npm publish --access public"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.4.0",
    "vitest": "^3.0.0"
  }
}
